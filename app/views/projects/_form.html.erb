<style>
  .button-green {
    background-color: green;
    border: none;
    color: white;
    padding: 0.8em 2em;
    text-align: left;
    text-decoration: underline;
    display: inline-block;
    font-size: 1em;
    margin: 0.25em 0.125em;
    cursor: pointer;
    border: 2px solid black;
  }
</style>


<%= form_with(model: project) do |form| %>
  <% if project.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(project.errors.count, "error") %> prohibited this project from being saved:</h2>

      <ul>
        <% project.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <h1 style="font-family: 'Arial', monospace;"><%= form.label :title, style: "display: block" %> </a>
    <%= form.text_field :title %>
  </div>

  <div>
    <h1 style="font-family: 'Arial', monospace;"> <%= form.label :description, style: "display: block" %> </h1>
    <%= form.text_area :description %>
  </div>

  <div>
    <%= form.submit 'Submit', :class => 'button-green' %>
  </div>
<% end %>


